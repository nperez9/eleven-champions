import React from 'react';
import { Container } from '../../library';

const Publish = () => {
  return (
    <Container>
      Contactata para publica
    </Container>
  );
};

export default Publish;
